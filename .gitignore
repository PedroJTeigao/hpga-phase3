# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg

# virtual environments
.venv/
venv/
env/
ENV/

# model weights / large binaries -- Ollama models themselves live outside
# this tree (~/ollama-models), this is a guard against accidental copies
*.bin
*.gguf
*.safetensors
*.pt
*.pth
models/

# compiled locally from vendored source (esmfold/tmalign/TMalign.cpp) --
# machine-specific, rebuild with the one-line g++ command in that file
esmfold/tmalign/TMalign

# OS/editor cruft
.DS_Store
